CREATE TABLE FRAME_SQL (KEY, SQL);
CREATE TABLE BOARD (NUM, HEAD, MAIN);
CREATE TABLE ID (ID, PIN, CNT);


delete from FRAME_SQL;
 
INSERT INTO FRAME_SQL VALUES ('COMM001D_01', 'SELECT
NUM, HEAD, MAIN
FROM BOARD
ORDER BY NUM
LIMIT 10 OFFSET cast([OFFSET] as integer) 
'
);

INSERT INTO FRAME_SQL VALUES ('COMM001D_02', 'SELECT
NUM, HEAD, MAIN
FROM BOARD
WHERE NUM = [NUM]'
);


INSERT INTO FRAME_SQL VALUES ('COMM001D_03', 'INSERT INTO BOARD 
(
NUM
, HEAD
, MAIN
) VALUES (
[NUM]
, [HEAD]
, [MAIN]
)'
)
;